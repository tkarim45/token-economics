# Optional — only needed for real-provider latency measurement (extras: claude / bedrock).
# The default --mock mode is fully key-free and deterministic.
ANTHROPIC_API_KEY=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=
